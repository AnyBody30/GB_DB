1. В структуре созданной БД,на мой взгляд, в таблице comments избыточая информация - идентификатор пользователя user_id, т.к. комментарий однозначно определяет
пользователя, оставившего комментарий, через таблицу posts и внешний ключ post_id.
2. Статусы запросов на дружбу лучше вынести в отдельную таблицу-справочник статусов запросов на дружбу и в таблице запросов на дружбу friend_requests хранить уже 
идентификатор статуса status_id, а не поле типа enum. Думаю, так быстрее будут отрабатывать запросы. Ну и масштабировать легче, вдруг появятся новые статусы ответов на 
запросы дружбы :)
3. Для идентифицирующих фотографий пользователей users.photo_id (по умолчаю должна заполняться NULL) нужна отдельная таблица с этими фото, которая будет содежать 
только id и имя файла с фото (users.photo_id будет внешний ключ на эту таблицу). Имеющуюся таблицу photos не рекомендуется использовать, т.к. в ней есть обратное отношение 
внешний ключ на user_id.
4. Имя сообщества communities.name должно быть уникальным, иначе риск дублирования сообществ.
5. Структура БД понятна.